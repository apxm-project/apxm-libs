# apxm-demos-benchmarks

Benchmark/demo scaffolder. Priority pack — covers the bookkeeping
overhead that otherwise gets skipped (prereg, claim card,
`.apxm/` output placement). The decision tree is captured in
`notes.md`; `skill.air` not yet compiled.
