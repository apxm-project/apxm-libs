# apxm-compiler-pass

Develop or debug AIS/MLIR compiler passes: TableGen op definitions,
C++ shims, Rust handlers, pipeline registration in
`build_pass_list()`.

## Install

```bash
dekk apxm libs install apxm-compiler-pass
```

## Status

Scaffold. `skill.air` lands once the runtime side ships.
