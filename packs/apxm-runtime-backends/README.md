# apxm-runtime-backends

Skills for runtime scheduling, backend routing, and vLLM dispatch
hints across `apxm-backends` and `apxm-runtime` crates.

## Install

```bash
dekk apxm libs install apxm-runtime-backends
```

## Status

Scaffold. `skill.air` lands once the runtime side ships.
