skill_id = "obra-superpowers-writing-plans"
version = "0.0.1"
display_name = "Writing Plans (obra port)"
description = "Use when you have a spec or requirements for a multi-step task, before touching code"
entry_flow = "obra_superpowers_writing_plans"
required_capabilities = []
timeout_ms = 300000
token_limit = 32768
isolation_policy = "sandboxed"
side_effect_policy = "sandboxed"

[[inputs]]
name = "task"
type = "string"
description = "Natural-language task to drive the workflow."
required = true

[[outputs]]
name = "result"
type = "json"
description = "Structured workflow output (per-node trace + final synthesis)."
