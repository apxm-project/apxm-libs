skill_id = "obra-superpowers-executing-plans"
version = "0.0.1"
display_name = "Executing Plans (obra port)"
description = "Use when you have a written implementation plan to execute in a separate session with review checkpoints"
entry_flow = "obra_superpowers_executing_plans"
required_capabilities = []
timeout_ms = 300000
token_limit = 32768
isolation_policy = "sandboxed"
side_effect_policy = "sandboxed"

[[inputs]]
name = "task"
type = "string"
description = "Natural-language task to drive the workflow."
required = true

[[outputs]]
name = "result"
type = "json"
description = "Structured workflow output (per-node trace + final synthesis)."
