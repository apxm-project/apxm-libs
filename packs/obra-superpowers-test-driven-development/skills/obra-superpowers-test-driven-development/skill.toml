skill_id = "obra-superpowers-test-driven-development"
version = "0.0.1"
display_name = "Test Driven Development (obra port)"
description = "Use when implementing any feature or bugfix, before writing implementation code"
entry_flow = "obra_superpowers_test_driven_development"
required_capabilities = []
timeout_ms = 300000
token_limit = 32768
isolation_policy = "sandboxed"
side_effect_policy = "sandboxed"

[[inputs]]
name = "task"
type = "string"
description = "Natural-language task to drive the workflow."
required = true

[[outputs]]
name = "result"
type = "json"
description = "Structured workflow output (per-node trace + final synthesis)."
