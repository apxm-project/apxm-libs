# apxm-bundle-example

Reference multi-skill pack. Demonstrates the unified pack format:
optional `agent.toml`, `hierarchy.toml`, `tools.toml` at the pack root,
plus one or more `skills/<id>/` entries.

This is a scaffold — no compiled `skill.apxmobj` ships yet.
